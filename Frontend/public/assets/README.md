# Assets

Static assets (SVG, images) for the HealthCare frontend.

- **svg/** – SVG icons and graphics (e.g. from Figma export)
- **images/** – PNG, JPG, and other raster images

Files are served by Next.js from `/assets/svg/` and `/assets/images/` (e.g. `/assets/images/photo.png`).
