package com.example.HealthCare.enums;

/**
 * Meal context attached to a blood glucose reading. Matches the ysalus
 * MetricTimeOfDay enum (BeforeMeal / AfterMeal).
 */
public enum MealContext {
    BEFORE_MEAL,
    AFTER_MEAL
}
