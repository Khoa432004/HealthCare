# ============================================================
# Cấu hình chung - dùng cú pháp ${VAR:default}
#   - Khi chạy local: dùng giá trị default
#   - Khi deploy (Northflank/Docker): đọc từ environment variable
# ============================================================

# Server
server.port=${PORT:8080}
spring.application.name=HealthCare
server.compression.enabled=true
server.compression.mime-types=application/json,application/xml,text/html,text/xml,text/plain

# ---- Database ----
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://pg-1ab1fafe-vkhanh1224-36a2.g.aivencloud.com:13012/defaultdb?sslmode=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:avnadmin}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:AVNS_NjBxjL3G-sKhhrDGuCm}
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000

# ---- JPA ----
spring.jpa.show-sql=false
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always
spring.sql.init.continue-on-error=true

# ---- JWT ----
security.jwt.secret=${SECURITY_JWT_SECRET:ZmFrZV9iYXNlNjRfc2VjcmV0X2NoYW5nZV9tZV9sYXRlcg==}
security.jwt.expiration-ms=${SECURITY_JWT_EXPIRATION_MS:86400000}
security.jwt.refresh-expiration-ms=${SECURITY_JWT_REFRESH_EXPIRATION_MS:604800000}

# ---- Email (Gmail SMTP) ----
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:donguyendangkhoa04031@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:wvrn ckom auve rmxb}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# ---- Cache ----
spring.cache.type=${SPRING_CACHE_TYPE:simple}

# ---- Actuator (health check endpoint cho Docker/Northflank) ----
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when-authorized

# ---- File upload ----
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# ---- Logging ----
logging.level.root=INFO
logging.level.com.example.HealthCare=INFO
logging.level.org.hibernate=WARN
